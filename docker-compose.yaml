services:
  gastos-apto:
    build: .
    restart: unless-stopped
    networks:
      coolify:
        ipv4_address: 172.19.0.100
    ports:
      - "3000:3000"

networks:
  coolify:
    external: true